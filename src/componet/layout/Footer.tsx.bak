"use client";

import Link from "next/link";
import { motion } from "framer-motion";
import { Shield, Phone, Mail, Clock, ChevronRight, MapPin, Facebook, Twitter, Linkedin, Instagram } from "lucide-react";
import { Button, Text, Title, ActionIcon } from "rizzui";

const Footer = () => {
  const currentYear = new Date().getFullYear();

  const footerLinks = [
    { label: "About Us", href: "/about" },
    { label: "Our Team", href: "/team" },
    { label: "Services", href: "/services" },
    { label: "Blog", href: "/blog" },
    { label: "Contact Us", href: "/contact" },
  ];

  const serviceSchedule = [
    { days: "Saturday - Monday", hours: "Open 24/7" },
    { days: "Tuesday - Thursday", hours: "Open 24/7" },
    { days: "Friday", hours: "Open 24/7" },
  ];

  const socialLinks = [
    { icon: Facebook, label: "Facebook", href: "https://facebook.com" },
    { icon: Twitter, label: "Twitter", href: "https://twitter.com" },
    { icon: Linkedin, label: "LinkedIn", href: "https://linkedin.com" },
    { icon: Instagram, label: "Instagram", href: "https://instagram.com" },
  ];

  return (
    <footer className="bg-primary-navy dark:bg-gray-950 text-white border-t border-gray-800 dark:border-gray-900">\n      {/* Main Footer */}
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 md:py-12 lg:py-16">
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 md:gap-8">
          {/* Company Info */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: "-100px" }}
            transition={{ duration: 0.5 }}
            className="space-y-4"
          >
            <div className="flex items-center space-x-3">
              <div className="p-2 bg-accent-gold rounded-lg">
                <Shield className="h-6 w-6 md:h-7 md:w-7 text-primary-navy" />
              </div>
              <div>
                <h2 className="text-xl md:text-2xl font-bold">
                  DMCA<span className="text-accent-gold">Master</span>
                </h2>
                <p className="text-sm text-gray-300">
                  Copyright Protection Experts
                </p>
              </div>
            </div>
            <p className="text-sm md:text-base text-gray-300 leading-relaxed">
              Protecting your digital content from copyright infringement, 
              piracy, and unauthorized use with expert DMCA takedown services.
            </p>
            
            {/* Social Links */}
            <div className="pt-2">
              <h4 className="text-sm font-semibold text-gray-300 mb-3">Follow Us</h4>
              <div className="flex flex-wrap gap-3">
                {socialLinks.map((social, index) => {
                  const IconComponent = social.icon;
                  return (
                    <motion.a
                      key={social.label}
                      href={social.href}
                      target="_blank"
                      rel="noopener noreferrer"
                      initial={{ opacity: 0, scale: 0.8 }}
                      whileInView={{ opacity: 1, scale: 1 }}
                      viewport={{ once: true }}
                      transition={{ delay: index * 0.1 }}
                      whileHover={{ y: -3, scale: 1.1 }}
                      className="p-2.5 bg-gray-800 dark:bg-gray-900 hover:bg-accent-gold text-gray-300 hover:text-primary-navy rounded-lg transition-all duration-300 group"
                      aria-label={social.label}
                    >
                      <IconComponent className="h-5 w-5 group-hover:scale-110 transition-transform" />
                    </motion.a>
                  );
                })}
              </div>
            </div>
          </motion.div>

          {/* Useful Links */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: "-100px" }}
            transition={{ duration: 0.5, delay: 0.1 }}
            className="space-y-4"
          >
            <h3 className="text-lg md:text-xl font-semibold text-accent-gold">
              Useful Links
            </h3>
            <ul className="space-y-2">
              {footerLinks.map((link, index) => (
                <motion.li
                  key={link.label}
                  initial={{ opacity: 0, x: -10 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: 0.2 + index * 0.1 }}
                >
                  <Link
                    href={link.href}
                    className="flex items-center text-sm md:text-base text-gray-300 hover:text-accent-gold transition-colors group py-2"
                  >
                    <ChevronRight className="h-3 w-3 md:h-4 md:w-4 mr-2 opacity-0 group-hover:opacity-100 transition-opacity" />
                    <span className="group-hover:translate-x-1 transition-transform">
                      {link.label}
                    </span>
                  </Link>
                </motion.li>
              ))}
            </ul>
          </motion.div>

          {/* Service Schedule */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: "-100px" }}
            transition={{ duration: 0.5, delay: 0.2 }}
            className="space-y-4"
          >
            <h3 className="text-lg md:text-xl font-semibold text-accent-gold">
              Service Schedule
            </h3>
            <div className="space-y-3">
              {serviceSchedule.map((schedule, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, x: -10 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: 0.3 + index * 0.1 }}
                  className="flex items-start space-x-3 bg-gray-800/30 p-3 rounded-lg"
                >
                  <Clock className="h-4 w-4 md:h-5 md:w-5 text-accent-gold mt-0.5 flex-shrink-0" />
                  <div>
                    <p className="text-sm md:text-base font-medium">{schedule.days}</p>
                    <p className="text-xs md:text-sm text-gray-300">{schedule.hours}</p>
                  </div>
                </motion.div>
              ))}
            </div>
          </motion.div>

          {/* Contact Info */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: "-100px" }}
            transition={{ duration: 0.5, delay: 0.3 }}
            className="space-y-4"
          >
            <h3 className="text-lg md:text-xl font-semibold text-accent-gold">
              Contact Info
            </h3>
            <div className="space-y-3">
              <motion.a
                href="tel:+923066768863"
                initial={{ opacity: 0 }}
                whileInView={{ opacity: 1 }}
                viewport={{ once: true }}
                transition={{ delay: 0.4 }}
                className="flex items-center space-x-3 text-gray-300 hover:text-accent-gold transition-colors p-2 hover:bg-gray-800/30 rounded-lg"
              >
                <Phone className="h-4 w-4 md:h-5 md:w-5 flex-shrink-0" />
                <span className="text-sm md:text-base">+92 306 676 8863</span>
              </motion.a>
              
              <motion.a
                href="mailto:help@dmcamaster.com"
                initial={{ opacity: 0 }}
                whileInView={{ opacity: 1 }}
                viewport={{ once: true }}
                transition={{ delay: 0.5 }}
                className="flex items-center space-x-3 text-gray-300 hover:text-accent-gold transition-colors p-2 hover:bg-gray-800/30 rounded-lg"
              >
                <Mail className="h-4 w-4 md:h-5 md:w-5 flex-shrink-0" />
                <span className="text-sm md:text-base">help@dmcamaster.com</span>
              </motion.a>
              
              <motion.div
                initial={{ opacity: 0 }}
                whileInView={{ opacity: 1 }}
                viewport={{ once: true }}
                transition={{ delay: 0.6 }}
                className="flex items-start space-x-3 p-2"
              >
                <MapPin className="h-4 w-4 md:h-5 md:w-5 text-accent-gold mt-0.5 flex-shrink-0" />
                <span className="text-sm md:text-base text-gray-300">
                  Global DMCA Protection Services
                </span>
              </motion.div>
            </div>
            
            {/* Call to Action */}
            <motion.div
              initial={{ opacity: 0, scale: 0.9 }}
              whileInView={{ opacity: 1, scale: 1 }}
              viewport={{ once: true }}
              transition={{ delay: 0.7 }}
              className="pt-4"
            >
              <h4 className="text-base font-semibold mb-3">
                24/7 Emergency Support
              </h4>
              <a
                href="tel:+923066768863"
                className="inline-flex items-center justify-center w-full px-4 py-3 bg-accent-gold text-primary-navy font-bold rounded-lg hover:bg-yellow-600 transition-colors duration-300 shadow-lg text-sm md:text-base"
              >
                <Phone className="h-4 w-4 md:h-5 md:w-5 mr-2" />
                Call Now
              </a>
            </motion.div>
          </motion.div>
        </div>

        {/* Divider */}
        <motion.div
          initial={{ scaleX: 0 }}
          whileInView={{ scaleX: 1 }}
          viewport={{ once: true }}
          transition={{ duration: 0.8 }}
          className="h-px bg-gradient-to-r from-transparent via-gray-600 to-transparent my-6 md:my-8"
        />

        {/* Copyright */}
        <motion.div
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          viewport={{ once: true }}
          className="text-center space-y-2"
        >
          <p className="text-xs md:text-sm text-gray-400">
            © {currentYear} DMCA Master. All rights reserved.
          </p>
          <p className="text-xs text-gray-500">
            Professional DMCA Takedown Service
          </p>
          <div className="flex flex-col sm:flex-row justify-center items-center space-y-2 sm:space-y-0 sm:space-x-4 text-xs text-gray-500">
            <Link href="/privacy" className="hover:text-gray-300 transition-colors">
              Privacy Policy
            </Link>
            <span className="hidden sm:inline">•</span>
            <Link href="/terms" className="hover:text-gray-300 transition-colors">
              Terms of Service
            </Link>
            <span className="hidden sm:inline">•</span>
            <Link href="/disclaimer" className="hover:text-gray-300 transition-colors">
              Disclaimer
            </Link>
          </div>
        </motion.div>
      </div>
    </footer>
  );
};

export default Footer;